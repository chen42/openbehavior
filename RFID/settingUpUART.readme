For Raspbian Jessie:
run the following two commands

sudo systemctl stop serial-getty@ttyAMA0.service
sudo systemctl disable serial-getty@ttyAMA0.service 

then disable the login shell:

sudo raspi-config

select 9 for advanced options.
select A8 for Serial to disable login shell.

then reboot.



